                                                                               
; autoPinSwap() results for schematic network.

swapPins("bufif1" "")

swapPins("write" "(p SEL WR)")

swapPins("sram_cell" "")

swapPins("inv1_3x" "")

swapPins("inv1_9x" "")

swapPins("sense_amp" "")

swapPins("mux4x1" "")

swapPins("dec2x4" "(p (f Y1 A) (f Y2 B))")

swapPins("precharge" "(p !BL BL)")
